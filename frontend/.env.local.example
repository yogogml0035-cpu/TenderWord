# TenderWord Frontend 环境变量模板
# 复制此文件为 frontend/.env.local 并按需调整

# 后端 API 地址（支持多个，逗号分隔；会按当前页面主机名优先匹配）
# 例如：本机访问时用 localhost，局域网访问时用 10.11.11.44
NEXT_PUBLIC_API_URL=http://localhost:8000,http://10.11.11.44:8000
