# Ralph Progress

Requirement: tasks/gngk-hw-zc-field-replacement-fix
Started: 2026-05-23 22:00
Source: prd.json

## Codebase Patterns

- `gngk_hw_zc_get_replacements` is the current graph wrapper for domestic public goods self-funded replacement extraction.
- `run_get_replacements` builds `placeholder_mapping` and `replacements` from extractor specs and replacement field specs; this task should reuse that mechanism.
- Core validation can use mocked document content and should not require a real external tender lookup API.
- Tender data fields that feed generation should be synchronized across `backend/models/tender.py`, `backend/states/base_state.py`, `backend/services/document_service.py` initial/rewrite state keys, `backend/util/common_util/fetch_tender_data.py`, `backend/api/tender.py`, and `frontend/types/api.ts`.

## 2026-05-23 23:49 - US-001
- 补齐了 `investment` 预算金额数据契约：后端 `TenderData`、公共 graph state、生成初始 state、rewrite snapshot keys、edit state、接口抓取与 tender API 响应都能携带该字段；前端 `TenderData` 类型新增可选 `investment`，保持旧数据兼容。
- 更改的文件：`backend/models/tender.py`、`backend/states/base_state.py`、`backend/services/document_service.py`、`backend/util/common_util/fetch_tender_data.py`、`backend/api/tender.py`、`frontend/types/api.ts`、`backend/tests/services/test_document_service_initial_state.py`、`backend/tests/util/test_fetch_tender_data.py`、`backend/tests/api/test_tender_api.py`、`tasks/gngk-hw-zc-field-replacement-fix/prd.json`。
- 验证：`backend\.venv\Scripts\python.exe -m pytest tests -v` 通过，`272 passed, 1 warning`；`npm run type-check` 通过；`npm run lint` 通过；`npm run test -- --runInBand --runTestsByPath __tests__/unit/lib/test_api.test.ts __tests__/unit/lib/test_form_data_converter.test.ts` 通过，`41 passed`。
- **未来迭代的学习：**
  - 新增 `TenderData` 字段不能只改 Pydantic model；如果后续 rewrite/edit 或会话恢复需要沿用该字段，也要同步 `REWRITE_STATE_KEYS` 和 `DocumentService` 的状态构建。
  - `fetch_tender_data` 返回字典后，`backend/api/tender.py` 会重新构造 `TenderData`，新增字段需要在这层显式透传，否则 API 响应会被默认值吞掉。
  - Windows 下跑前端 Jest 单文件时，使用 `--runTestsByPath` 加正斜杠路径更稳定；反斜杠 pattern 可能出现 `No tests found`。
---
