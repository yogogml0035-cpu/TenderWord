# Ralph Progress

Requirement: tasks/comment-supplement-comment-agent
Started: 2026-05-30 00:00
Source: prd.json

## Codebase Patterns
- 后端任务类型需要同时更新内部 `backend/task/task_queue_manager.py::TaskKind`、API `backend/models/task.py::TaskKind` 和 `backend/services/task_service.py` 的转换映射。
- SSE `done` 新增结构字段时，需要同步后端模型 `backend/models/sse.py`、发送入口 `backend/core/sse_manager.py`、前端 `frontend/types/api.ts` 与类型契约测试。
- Windows/WSL 共用工作区时，前端浏览器验证前要确认 Linux 原生 optional 依赖已安装；若 `lightningcss` 或 `@tailwindcss/oxide` 只存在 win32 包，在 `frontend/` 运行 `npm install --include=optional` 后重启 dev server。
- 批注写回 summary/warning 规则统一走 `backend/nodes/common_word_nodes/comment_writeback.py::build_comment_writeback_summary_payload`；update_word 节点只在 `generated > 0 && failed > 0` 时用 `progress_log.warning()`，不要重新加入 0 写入硬失败。
- `DocumentService._build_rewrite_state_snapshot()` 可以保存后端内部批注依据长数组和 `generation_mode`；`_build_task_result_payload()`、SSE `done` 和前端 `chat-storage` 只能保留白名单摘要字段，不能泄露 `comment_plan_detail`、`strikethrough_plan`、`non_black_font_plan`。
- 新增 Prompt Layer builder 时同步补 `backend/prompts/types.py` 输入 dataclass、`backend/prompts/__init__.py` 导出和 `backend/tests/prompts/test_*.py` 契约测试；Prompt Layer 仍只做纯渲染，不接日志、LLM 或 Word 副作用。
- `comment_agent` 采用“纯确定性函数 + LangChain StructuredTool factory”的分层：校验只读取 `polished_text`，Word 写入工具必须重新校验并限制在 `bound_start/bound_end` 内，不复用全文兜底写回。

## 2026-05-30 13:08 - US-001
- 建立了 `comment_supplement` 任务类型契约：后端内部/API TaskKind、TaskService 映射、前端 TaskKind union 均已支持。
- 建立了 `comment_writeback` 摘要契约：后端 `DoneEventData`、`SSEManager.send_done` / `send_done_threadsafe`、前端 `TaskResult` / `SSEDoneEvent` 均可透传 summary/generated/added/failed/skipped/warning。
- 更改的文件：`backend/task/task_queue_manager.py`、`backend/models/task.py`、`backend/models/sse.py`、`backend/models/__init__.py`、`backend/services/task_service.py`、`backend/core/sse_manager.py`、`frontend/types/api.ts`、`frontend/lib/api.ts`、`frontend/hooks/useChatSSE.ts`、`backend/tests/services/test_task_service_task_kind.py`、`backend/tests/services/test_sse_manager_agent_step.py`、`backend/tests/models/test_sse_agent_step.py`、`frontend/__tests__/unit/types/test_api_sse_agent_step.test.ts`、`tasks/comment-supplement-comment-agent/prd.json`、`tasks/comment-supplement-comment-agent/progress.txt`。
- 验证：`backend/.venv-linux/bin/python -m pytest backend/tests/services/test_task_service_task_kind.py backend/tests/services/test_sse_manager_agent_step.py backend/tests/models/test_sse_agent_step.py -v` 通过；`npm test -- --runTestsByPath __tests__/unit/types/test_api_sse_agent_step.test.ts` 通过；`npm run lint` 通过；`npm run type-check` 通过；本轮触达文件的 `git diff --check -- ...` 通过。
- 全仓 `git diff --check` 因进入本轮前已有的 `backend/.env.example`、`scripts/start-dev.ps1` trailing whitespace 失败，未改动这些既有文件。
- **未来迭代的学习：**
  - 任务类型是双枚举结构，新增 task kind 时不要只改 API 类型，还要改队列内部枚举和 TaskService 映射。
  - SSE done 的结构字段需要同时覆盖 Pydantic 模型、SSEManager 直接发送入口和前端 API 类型，避免断线重放/任务状态/前端卡片三处契约漂移。
  - 当前工作树已有与本需求无关的 `.env.example` 和 PowerShell 脚本空白改动，提交时应只暂存本轮文件。
---
## 2026-05-30 13:31 - US-001
- 修复了 Validator 上次记录的浏览器验证环境问题：`frontend/node_modules` 只有 Windows 原生 optional package，已通过 `npm install --include=optional` 恢复 Linux 平台的 `lightningcss-linux-x64-gnu` 与 `@tailwindcss/oxide-linux-x64-gnu`。
- 重启前端 dev server 后验证 `/tender` 页面返回 200，Playwright 打开页面成功，DOM 不再是 Next error，console 无 error/warning；截图保存到 `tasks/comment-supplement-comment-agent/screenshots/validator-us-001-pass-1.png`，console 和 requests 证据保存到 `tasks/comment-supplement-comment-agent/logs/validator-us-001-retry-console.log`、`tasks/comment-supplement-comment-agent/logs/validator-us-001-retry-requests.log`。
- 更改的文件：`tasks/comment-supplement-comment-agent/prd.json`、`tasks/comment-supplement-comment-agent/progress.txt`。
- 验证：`backend/.venv-linux/bin/python -m pytest backend/tests/services/test_task_service_task_kind.py backend/tests/services/test_sse_manager_agent_step.py backend/tests/models/test_sse_agent_step.py -v` 通过；`npm test -- --runTestsByPath __tests__/unit/types/test_api_sse_agent_step.test.ts` 通过；`npm run lint` 通过；`npm run type-check` 通过；`playwright-cli -s=validator-us-001-retry open http://localhost:8502/tender`、截图、console、requests 采集通过；`curl -sS -L -o /tmp/tenderword_tender_probe.html -w '%{http_code}\n' http://localhost:8502/tender` 返回 200。
- **未来迭代的学习：**
  - Windows 与 WSL 交替使用同一个 `frontend/node_modules` 时，平台原生 optional dependency 可能停留在 win32 版本，Next/Tailwind 编译会报缺少 Linux `.node` 文件；用锁文件恢复 optional 依赖后要重启 dev server。
  - 根路径 `/` 当前会 307 重定向到 `/tender`，浏览器或 curl 健康检查应检查 `/tender` 或启用跟随跳转，避免把正常 redirect 当失败。
  - 当前工作树已有与本需求无关的 `backend/.env.example` 和 `scripts/start-dev.ps1` 空白改动，提交时仍应只暂存本轮需求文件。
---
## 2026-05-30 14:04 - US-002
- 将现有批注写回硬失败降级为 warning：common、gjgk、gngk_fw_zc、gngk_hw_cz 四条 update_word 路径不再在 `generated_comment_count > 0` 且 0 条写入时抛异常，改为写入 `comment_writeback_result` 摘要并在失败数大于 0 时输出 warning 级进度。
- 新增统一 `build_comment_writeback_summary_payload`，集中计算 `summary/generated/added/failed/skipped/warning`，确保 `generated=0` 和 skipped-only 不触发 warning。
- 同步修正批注写回相关测试、根级 AGENTS 约束和 `asset/shared_runtime_word_skill_knowledge_pack.md` 中的长期规则。
- 更改的文件：`AGENTS.md`、`asset/shared_runtime_word_skill_knowledge_pack.md`、`backend/nodes/common_word_nodes/comment_writeback.py`、`backend/nodes/common_word_nodes/update_word.py`、`backend/nodes/gjgk_word_nodes/gjgk_update_word.py`、`backend/nodes/gngk_word_nodes/gngk_fw_zc_update_word.py`、`backend/nodes/gngk_word_nodes/gngk_hw_cz_update_word.py`、`backend/tests/nodes/test_comment_writeback.py`、`backend/tests/nodes/test_update_word_inline_style_writeback.py`、`backend/tests/nodes/test_gngk_hw_cz_direct_replace_word.py`、`tasks/comment-supplement-comment-agent/prd.json`、`tasks/comment-supplement-comment-agent/progress.txt`。
- 验证：`backend/.venv-linux/bin/python -m pip install -r backend/requirements.txt` 补齐 Linux venv 中缺失的声明依赖；`backend/.venv-linux/bin/python -m pytest backend/tests/nodes/test_comment_writeback.py backend/tests/nodes/test_update_word_inline_style_writeback.py backend/tests/nodes/test_gngk_hw_cz_direct_replace_word.py backend/tests/nodes/test_gngk_fw_zc_update_word.py -v` 通过；`backend/.venv-linux/bin/python -m pytest backend/tests -v` 通过，352 passed；`npm run lint` 通过；`npm run type-check` 通过；本轮变更文件 `git diff --check -- ...` 通过。
- **未来迭代的学习：**
  - 批注写回统计现在有统一 builder，新增 task result 或 SSE 透传时应复用 `comment_writeback_result` / builder 输出，不要在 DocumentService 或前端重新推断 warning。
  - 0 条成功写入不再等价任务失败；真正应阻断的仍是正文写回、受保护字段、锁定范围和保存等主流程强契约。
  - Linux venv 若只装了部分 LangChain 依赖，导入 `backend.nodes.common_word_nodes` 会因 `deepagents` 缺失在测试收集阶段失败；按 `backend/requirements.txt` 补齐后再跑后端测试。
---
## 2026-05-30 14:29 - US-003
- 扩展了任务结果与 SSE done 主干：`DocumentService._build_task_result_payload()` 现在透传 `comment_writeback` 摘要，并继续用白名单输出，避免把三组批注依据长数组放进 result；任务完成时本地 callback 和 `SSEManager.send_done_threadsafe()` 都会携带同一份 `comment_writeback`。
- 扩展了后端会话 `rewrite_state`：generate 成功快照会保留 `comment_plan_detail`、`strikethrough_plan`、`non_black_font_plan` 和 `generation_mode`，供后续补批注、rewrite/edit 链路读取；edit/rewrite 初始 state 仍不注入 `generation_mode`。
- 前端 `chatStore` 持久化前会清洗消息 metadata 中的三组内部批注依据字段，确保下载卡 metadata 和 `sessionStorage` 不保存长数组。
- 同步更新了 `asset/shared_runtime_word_skill_knowledge_pack.md`，记录内部 rewrite_state 与外部 result/SSE/前端持久化的边界。
- 更改的文件：`backend/services/document_service.py`、`backend/tests/services/test_document_service_task_result.py`、`backend/tests/services/test_document_service_agent_step.py`、`frontend/stores/chatStore.ts`、`frontend/__tests__/unit/stores/test_session_persistence.test.ts`、`asset/shared_runtime_word_skill_knowledge_pack.md`、`tasks/comment-supplement-comment-agent/prd.json`、`tasks/comment-supplement-comment-agent/progress.txt`。
- 验证：`backend/.venv-linux/bin/python -m pytest backend/tests/services/test_document_service_task_result.py backend/tests/services/test_document_service_agent_step.py backend/tests/services/test_sse_manager_agent_step.py backend/tests/models/test_sse_agent_step.py backend/tests/services/test_document_service_initial_state.py -v` 通过；`backend/.venv-linux/bin/python -m pytest backend/tests -v` 通过，355 passed；`npm test -- --runTestsByPath __tests__/unit/stores/test_session_persistence.test.ts` 通过；`npm test -- --runInBand` 通过，31 suites / 355 tests；`npm run lint` 通过；`npm run type-check` 通过；本轮变更文件 `git diff --check -- ...` 通过。
- **未来迭代的学习：**
  - 任务 result / SSE done 是前端可见白名单摘要层，新增内部 state 字段时不要顺手透传到 `_build_task_result_payload()`。
  - `rewrite_state` 是后端会话内部上下文，可以保存后续任务需要的长数组；前端 `sessionStorage` 应通过 store `partialize` 或 metadata 白名单继续阻断这些长数组。
  - `comment_writeback` 应直接复用 update_word 写回 state 的 `comment_writeback_result`，缺失时再用统一 builder 从计数字段补齐摘要。
---
## 2026-05-30 14:44 - US-004
- 新增无参考批注 prompt 契约：`backend/prompts/comment_no_reference_prompt.py` 提供三维审查、纯净 JSON 数组输出和 `reference_text` 精确来自 `polished_text` 的约束，不引入历史参考、送审稿差异、删除线或非黑色字体计划逻辑。
- 补齐 Prompt Layer 输入类型与统一导出，新增 prompt 单测覆盖三维审查、JSON 契约、锚点约束、无参考逻辑隔离和未知类型错误。
- 按 Prompt Layer 回写路由更新 `asset/shared_runtime_word_skill_knowledge_pack.md` 与 `asset/README.md`，记录无参考批注 prompt 的长期边界与测试入口。
- 更改的文件：`backend/prompts/comment_no_reference_prompt.py`、`backend/prompts/types.py`、`backend/prompts/__init__.py`、`backend/tests/prompts/test_comment_no_reference_prompt.py`、`asset/shared_runtime_word_skill_knowledge_pack.md`、`asset/README.md`、`tasks/comment-supplement-comment-agent/prd.json`、`tasks/comment-supplement-comment-agent/progress.txt`。
- 验证：`backend/.venv-linux/bin/python -m pytest backend/tests/prompts/test_comment_no_reference_prompt.py backend/tests/prompts/test_comment_prompt_reference_contract.py -v` 通过；`backend/.venv-linux/bin/python -m pytest backend/tests -v` 通过，360 passed；`npm run lint` 通过；`npm run type-check` 通过；本轮变更文件 `git diff --check -- ...` 通过。
- **未来迭代的学习：**
  - 无参考批注 prompt 只能消费 `polished_text`，后续 comment_supplement/comment_agent 接入时不要把 `comment_plan_detail`、`strikethrough_plan` 或 `non_black_font_plan` 塞回该 prompt。
  - gngk 子类型仍通过 `get_tender_type_family()` 收敛到 `gngk` prompt registry，新增 prompt builder 要沿用 family 归一化而不是枚举所有后端 form_type。
  - Prompt Layer 变更也要更新共享知识包；若只新增同一主题内规则，`asset/README.md` 更新适用范围即可，不需要新建知识包。
---
## 2026-05-30 15:04 - US-005
- 新增 `comment_agent` 运行时包：固定 `name="comment_agent"` 调用 `langchain.agents.create_agent`，并用 `ToolCallLimitMiddleware` 将 `validate_comment_references` 限制为最多 3 次、`write_validated_comments_to_word` 限制为最多 1 次。
- 实现确定性校验与写入工具：AI 只能按同 index 修改 `reference_text`，`comment_text` 必须保持初始 JSON 原样；校验只基于 `polished_text`，失败反馈包含 index、原 reference、失败原因和相近片段；写入工具会重新校验，只在 Word 锚点边界内写入无既有批注的通过项。
- 增加 `comment_agent` 审计日志：记录初始 JSON、AIMessage 内容、每轮校验结果、最终 passed/failed/skipped 和 Word 写入统计；`agent_step` 只转发 AIMessage 内容，过滤工具消息。
- 更改的文件：`backend/agents/comments/__init__.py`、`backend/agents/comments/comment_agent.py`、`backend/agents/comments/tools.py`、`backend/agents/comments/types.py`、`backend/agents/comments/workspace.py`、`backend/tests/agents/test_comment_agent.py`、`asset/shared_runtime_word_skill_knowledge_pack.md`、`asset/README.md`、`tasks/comment-supplement-comment-agent/prd.json`、`tasks/comment-supplement-comment-agent/progress.txt`。
- 验证：`backend/.venv-linux/bin/python -m pytest backend/tests/agents/test_comment_agent.py -v` 通过；`backend/.venv-linux/bin/python -m pytest backend/tests/agents/test_comment_agent.py backend/tests/nodes/test_comment_writeback.py -v` 通过；`backend/.venv-linux/bin/python -m pytest backend/tests -v` 通过，368 passed；`npm run lint` 通过；`npm run type-check` 通过；`npm test -- --runInBand` 通过，31 suites / 355 tests；本轮改动文件的 staged `git diff --cached --check` 通过。
- 全仓 `git diff --check` 仍因进入本轮前已有的 `backend/.env.example`、`scripts/start-dev.ps1` trailing whitespace 失败，未改动这些既有文件。
- 本 story 只新增后端运行时与测试，无 UI 改动，未执行浏览器验证。
- **未来迭代的学习：**
  - `comment_agent` 的 LangChain 工具限制按工具名配置；后续接入 graph 时必须继续使用 `validate_comment_references` 和 `write_validated_comments_to_word` 这两个稳定工具名。
  - 批注候选校验不要把 Word 文档当兜底来源；AI 修复锚点的唯一文本依据是 `polished_text`，Word 工具只负责在已限定锚点边界内写入。
  - 工具消息和排障细节只进审计日志，前端过程卡只能展示 `AIMessage.content`。
---
