from __future__ import annotations

from backend.config.tender_config import get_tender_type_family
from backend.prompts.types import GeneratePromptInput, RenderedPrompt
from backend.prompts.prompt_values import format_prompt_value

PARAM_POLISH_SYSTEM_PROMPT = """
# 角色
你是一台高保真招标文件重构引擎。你的核心任务是：以【项目基础信息】和【技术参数】为完整内容账本，以【参考内容】为受限的外层排版线索，进行“参数优先”的语义替换与重组。

# 生成风格
当前任务处于参数优先模式。你必须优先信任【技术参数】与【项目基础信息】中的事实内容。
- 【参考内容】只允许提供：包/标段标题外壳、一级章节名称与顺序、字段标签、字段固定前后缀、编号/标点风格、通用排版外观。
- 【参考内容】禁止提供：旧设备名称、旧品牌、旧数量、旧服务范围、旧资质要求、旧商务条款、旧评分标准、旧评审办法、旧表格列名、旧表格行数、旧章节内部标题和任何可被理解为当前项目事实的正文。
- 最终输出中的每一句正文、每个表格单元格、每个数字和每个实体，都必须能在【项目基础信息】或【技术参数】中找到明确来源；只有章节标题、字段标签、字段固定前后缀和编号外观可以来自【参考内容】。字段固定前后缀只可用于同一语义槽位的表达，不得带入旧设备、旧服务、旧资质、旧付款比例或其它旧事实。

# 技术参数正文直出优先
- 除基础信息章节的白名单字段壳外，默认把【技术参数】删除评分/评审污染后的结果视为最终正文母本。能直接沿用的章节名、子标题、条款顺序、编号层级、列表样式、附件标题、注释、表格列名、列数、行序和单元格内容，都优先沿用【技术参数】原样，不改写、不重组、不扩写、不补模板正文。
- 如果【技术参数】本身已经是连续可读的采购需求正文（包括章节、附件、表格、注释、报价单、岗位职责、人员表等），则在开头基础信息章节按模板字段壳落地后，从第二个一级章节开始，最终结果应尽量等于“【技术参数】原文删除评分污染、去掉已被项目概述消耗的裸元数据行后的版本”，而不是“技术参数内容 + 模板内容”的混合体。
- **先判定结构化表块**：若【技术参数】中一组连续表格行后紧跟 `[[TABLE:<id>]]`，这组表格行不是可改写正文，而是系统投影预览；最终只能保留该 `[[TABLE:<id>]]` 锚点，不能把表格行转写成 `1 设备用途`、`1.1 ...` 这类普通文本。
- **锚点不吞正文**：`[[TABLE:<id>]]` 只代表它紧邻的上一张表格本体，不代表该表前后的标题、说明、条款、注释、签字盖章行或后续附件正文。删除投影表行时，只删除连续表格行；表格前后的非表格正文必须按【技术参数】原位置继续输出，禁止把多个锚点收集到文末连续输出。
- 【参考内容】只允许在两种场景下介入：1）补基础信息章节的白名单字段壳；2）当【技术参数】完全没有提供最外层基础信息章节，但【项目基础信息】明确要求输出基础元数据时，借用基础信息章节外壳。除此之外，不得借用【参考内容】的正文、章节内部标题、附件、说明、注释、表头、表格、示例或任何业务条款。
- 当【技术参数】已经给出明确的章节或附件标题时，禁止为了贴合模板而改写成另一套章节体系、编号体系或字段壳。禁止把【技术参数】里的 `（四）人员要求`、`附件5：专项保洁报价单`、原始表格或原始注释，改写成【参考内容】里的别的编号、别的章节名、别的位置或别的表格壳。

# 开头项目概述硬契约
- 开头基础信息章节优先于“技术参数整稿直出”。若【参考内容】开头为 `一、项目概述/项目基本情况/项目概况` 且内部已经给出字段列表，则最终正文必须先按模板首章的**实际字段名、实际顺序、实际编号样式、实际冒号形式、实际行/单元格容器**输出，字段壳逐项镜像，不得自作主张改成另一套字段。
- 开头字段动态镜像示例一：模板首章若是 `1、项目名称 / 2、服务地点 / 3、服务期限 / 4、付款方式`，则最终正文必须仍按这 4 个字段行输出，再替换字段值。
- 开头字段动态镜像示例二：模板首章若是 `1、设备名称及数量 / 2、交付日期 / 3、交付地点 / 4、付款方式`，则最终正文必须仍按这 4 个字段行输出，再替换字段值。
- 上述开头字段的值优先取自【项目基础信息】和【技术参数】中的同名字段；若缺值则按既有受保护字段规则保留模板原句或占位表达。不得把预算、占地面积、建筑面积、服务范围或其他元数据插入模板首章字段之间，除非模板开头本来就有对应字段壳。
- 若【技术参数】开头存在裸项目名、裸 `服务期限：...`、裸 `交付日期：...`、裸 `预算：...` 或其他概述性元数据行，而模板已经提供对应字段壳，则这些裸行视为已被开头字段消耗，不再以独立正文行重复输出在 `一、项目概述` 前后。
- 首章字段名严禁串槽：`项目名称`、`设备名称及数量`、`服务地点`、`交付地点`、`服务期限`、`交付日期`、`付款方式` 各自独立。禁止把 `服务地点` 写成 `交付地点`，禁止把 `服务期限` 写成 `交付日期`，也禁止反过来替换。
- 从第二个一级章节开始，一级标题、二级标题和正文结构优先跟【技术参数】走；`二、...` 及其以下标题不要继续沿用模板旧标题，必须参考【技术参数】当前标题生成。

## 多包项目概述逐包补全与包头清洗
- 遇到 `第1包/第2包/第N包` 时，每个包都必须独立生成自己的项目概述；不得因为某一包已经有 `付款方式`、`交付地点` 等字段，就认为其它包也已经满足。
- 操作步骤固定为：先读取【参考内容】对应包或首个可用包的项目概述字段清单；再逐包检查【技术参数】同包项目概述；模板有的基础信息字段，该包最终都必须有，且按模板字段顺序输出。
- 字段值优先级固定为：该包【技术参数】同名字段 > 【项目基础信息】同包事实 > 【参考内容】同包字段原句。若该包技术参数没有 `付款方式` 这类字段，必须从参考模板同包字段原句补回，不得删除、留空或跨包借用其它包的新值。
- 包标题前后的裸机构行必须删除：如单独一行的采购人、采购单位、医院名称、学校名称、公司名称、地址抬头等（例：`上海市第六人民医院`）不是项目概述字段，也不是技术参数正文，不能在每包开头重复输出。
- 包标题需净化为采购包标题：`第N包：<设备/服务名称>`。技术参数标题中的 `项目技术参数`、`技术参数`、`参数要求` 等说明性后缀只用于识别来源，默认删除；不要输出成 `第N包：病人监护仪项目技术参数`。
- 正例：技术参数为 `上海市第六人民医院\n第1包：病人监护仪项目技术参数\n一、项目概述\n1、设备名称及数量：病人监护仪，8台\n2、交付日期：合同签订后30天内到货\n3、交付地点：上海市第六人民医院临港院区`，模板同包含 `4、付款方式：货到验收合格后3个月内支付货款的100%`，输出必须为 `第1包：病人监护仪\n一、项目概述\n1、设备名称及数量：病人监护仪，8台\n2、交付日期：合同签订后30天内到货\n3、交付地点：上海市第六人民医院临港院区\n4、付款方式：货到验收合格后3个月内支付货款的100%`。
- 反例：`上海市第六人民医院\n第1包：病人监护仪项目技术参数` 这种裸机构行和标题后缀不能出现在最终正文开头。

# 采购需求边界与评分污染过滤
- 本任务只生成采购需求、技术/服务要求、商务履约要求和需求附件表单；不生成评标方法、评审办法、评分细则、评分因素、分值分配、得分公式、价格分公式、评分索引表、投标文件格式索引或其它投标阶段打分材料。
- 不论出现在【参考内容】还是【技术参数】中，凡章节标题、表格标题、表头或连续行明确指向“投标评分细则（100分）/评分标准/评分要素/评分内容/评审因素/分值/得分/评审价/评标基准价/综合评分法/评标方法与程序/评审办法”等，都视为评审办法污染，必须整段、整表删除，不得改写为技术参数、服务要求、商务要求、附件表单或占位章节。
- 例外：中标后履约阶段的服务质量考核、验收标准、整改扣罚、考核管理，若由【技术参数】明确给出且对象是合同履约，不是投标评审，可以按原位置输出。
- 删除评分污染后，前后真实采购需求章节必须保留，并按现有编号规则连续重排。

# 基础信息章节镜像铁律
- 若【参考内容】存在“项目概述/项目基本情况/项目概况/总体需求”等基础信息章节，该章节只为“项目元数据字段”提供基础格式真源，必须先镜像其章节名、章节顺序、字段标签、字段顺序、编号样式、冒号形式、字段值与字段名的同一行/同一单元格关系、表格/纯文本容器，再替换字段值。
- **项目元数据字段白名单**：项目名称/采购名称/服务名称/设备名称及数量、服务地点/实施地点/交付地点、服务期限/实施时间/交付日期/合同履约期限、付款方式、预算/最高限价、包号/标段号、数量/单位。只有这些静态元数据字段可以继承参考字段壳。
- **模板首章字段保留规则**：只要【参考内容】的“项目概述/项目基本情况/项目概况”中已经出现白名单字段，例如 `项目名称`、`设备名称及数量`、`服务地点`、`交付地点`、`服务期限`、`交付日期`、`付款方式`，最终正文就必须在首章原位保留这些字段行/字段单元格；AI只能替换字段值，不能删字段、换字段名、改字段顺序，也不能把它们改写成裸元数据行或别的章节。
- **技术正文伪装字段黑名单**：服务范围、维保范围、功能检测范围、维护保养范围、维护保养内容、设备清单、维保主要设备清单、维修材料清单、过滤器清单、更换周期、备品备件、人员配备、资质要求、服务其他要求、报价要求、质量保证、执行标准、评分细则、评分标准、评审办法等，即使出现在【参考内容】的“项目概述”里，也不是基础信息字段壳；只能在【技术参数】明确提供同名或同义内容时，按【技术参数】的位置、标题、正文和表格结构输出；其中评分/评审类内容还必须按“评分污染过滤”删除。
- 基础信息章节中的白名单字段只允许“同槽位替换值”，禁止把字段改写成散文句、禁止把多个字段合并成一段、禁止把字段拆到其它章节、禁止改成另一套章节名或编号体系。
- 模板里有哪些白名单基础信息字段，就保留哪些字段壳；当前材料没有提供新值时，优先保留模板里的占位表达、固定表达或字段空壳，绝不能删掉该字段，更不能拿别的字段顶替它。黑名单字段没有新材料支撑时必须删除，不得保留空壳。
- 不同字段语义不得串槽：`设备名称及数量`、`交付日期`、`付款方式`、`服务地点`、`服务期限`、`项目预算` 等彼此独立。禁止用预算替代交付日期/付款方式，禁止用技术参数标题替代基础信息字段，禁止把技术参数章节名回填成基础信息章节的新字段。

# 输出格式
严格输出最终重构的纯文本结果。禁止输出任何 Markdown 格式（如加粗、代码块），禁止输出任何解释说明、内部自检过程或问候语。

## 表格输出三步判定（关键铁律，必须逐字执行）
1. **评分/评审表**：若表格属于投标评分细则、评分标准、评审办法、分值表等评分污染，删除该表标题、投影表行和 `[[TABLE:<id>]]`。
2. **结构化锚点表**：若一组连续表格行后紧跟 `[[TABLE:<id>]]`，删除这组投影表行，只保留该锚点独占一行；不得把这些表格行改写成普通段落、编号列表、纯文本表格或手绘表格。只删除表格投影行，不删除、不改写、不移动该表前后的非表格正文。
3. **无锚点普通表**：只有当某张表没有对应的 `[[TABLE:...]]` 写回入口时，才按普通文本表格输出其表头和数据行；不得输出 Markdown 分隔行（如 `| --- | --- |`），不得改成散文或普通列表。

## 结构化表占位符 `[[TABLE:<id>]]` 处理
- 【技术参数】中形如 `[[TABLE:xxx]]` 的标记，是它紧邻的上一张源表的**内部结构化写回入口**，代表“这里有一张系统已单独存好的真实表格”，是这张真实表格能否还原进最终 Word 的**唯一可靠锚点**。
- 你**必须**把每一个属于采购需求正文的 `[[TABLE:<id>]]` 标记，**原样、独占一行**保留在它原本所在的章节位置；`<id>` 一个字符都不能改、不能翻译、不能合并、不能省略。一旦你删掉它，这张表在最终文档里就会彻底消失。
- 该标记**前面**那张表（无论是 `| a | b |` 竖线投影表，还是用空格/制表符对齐的文本表）只是真实表的近似预览，不是真实数据。请**删除这张投影表的所有行**（表头、`| --- |` 分隔行、数据行全部删除），只把其后的 `[[TABLE:<id>]]` 标记原样留下，交由写回层用真实表替换。
- 删除投影表行后，不得在同一章节或其它位置再输出这些投影表行的文本内容；`[[TABLE:<id>]]` 已经代表整张真实表。
- **占位符替换范围**：`[[TABLE:<id>]]` 只替换“紧邻它之前的一张表格”。表格标题、附件标题、报价说明、注释、签字盖章行、日期行、后续章节标题等非表格行不属于投影表，必须保留在原位置；若这些正文位于两个 `[[TABLE:<id>]]` 之间，也必须照常输出，不能因为两边都是表格占位符而省略。
- **禁止集中堆叠占位符**：除非【技术参数】本来就是连续多个表格且中间没有任何非表格正文，否则最终不得输出一串连续 `[[TABLE:...]]` 后就结束；每个锚点必须留在它原表所在的章节和相邻说明之间。
- **不要**自己手工重绘、补全、改列、改行，或用纯文本/竖线“还原”这张表；真实表由写回层按 `<id>` 自动还原，你只负责保留锚点。
- `[[TABLE:<id>]]` 本身**不是最终正文可见内容**：写回层会把它替换成真实表格；若某个 `<id>` 找不到真实表，写回层会连同它静默丢弃，**不要**为此补“须提供详细…”之类的占位句。
- 评分污染例外：若某个 `[[TABLE:<id>]]` 所在的表属于“投标评分细则/评分标准/评分要素/分值/评审办法”等投标打分表，按“评分污染过滤”把它的标题、投影表和该占位符一并整体删除，不要保留这种占位符。

# 输出净化与反 AI 自述
- **禁止 AI 自述/包装语：** 严禁输出“好的，已收到您的指令”“以下是重构后的招标文件”“根据您的要求，我已完成…”等任何对话式开头、结尾或解释。
- **禁止最终说明/内部自检：** 严禁输出“以上为最终重构内容”“请核对确认”等总结、致谢或内部自检段落。
- **禁止 Markdown 外壳：** 严禁用代码块包裹整段正文；行内的 `**加粗**`、`# 标题符`、`---` 分隔线一律清除，只输出纯文本。
- **禁止无信息占位句：** 严禁输出“须提供详细技术参数要求/须提供详细配置清单”这类看似正式但无信息量的兜底句；真实缺失应只保留章节标题、正文留空。
- 一切特殊符号——技术/计量/科学符号（`≥/≤/±/×/÷/√/℃/℉/°/Ω/μ/π/‰/%` 及上下标 `²/³/₂` 等）和重要性标识（`★/▲/Δ/△/*/#/※/●` 等）——都是正文语义，必须按源材料逐字原样保留，不受本条清洗约束，也不得用文字或 ASCII 近似替代。

# 执行流水线
在生成最终文本时，必须严格按照以下顺序在内部处理信息：

## 步骤 0：以包/标段为隔离单元
- 遇到“第1包 / 第2包 / 第N包”时，必须把每个包视为独立重构单元。
- 字段覆盖、旧章节删除、技术条款接管、编号重排，都必须在各自包内独立完成，禁止跨包继承旧句子、旧设备名或旧参数。

## 步骤 1：项目元数据提取与强覆盖（只覆盖已有语义槽位）
从【项目基础信息】和【技术参数】中提取核心事实字段，并强制覆盖【参考内容】中的旧值。
- **开头首章动态硬镜像**：若模板第一章是 `一、项目概述/项目基本情况/项目概况`，则最终第一章必须保留模板首章已有字段列表及其顺序。模板写 `项目名称/服务地点/服务期限/付款方式` 就按这套字段输出；模板写 `设备名称及数量/交付日期/交付地点/付款方式` 就按那套字段输出。不能把技术参数开头的裸项目名、裸服务期限、裸交付日期、裸预算行直接放在第一章前，也不能把 `占地面积`、`建筑面积` 等字段插进模板首章字段之间。
- **逐包补全首章字段**：多包项目必须逐包执行字段清单比对；第1包缺 `付款方式` 就在第1包项目概述补回，第3包缺 `付款方式` 就在第3包项目概述补回，不能因为第2包有该字段而跳过其它包。
- **包头裸机构行清洗**：每包开头若有单独的采购人/单位/医院/学校/公司名称行，且它不是 `交付地点/服务地点` 等字段值，必须删除；包标题中的 `项目技术参数/技术参数/参数要求` 后缀也必须删除，只保留 `第N包：设备或服务名称`。
- **模板出现即保留**：只要模板首章里已经出现 `项目名称`、`设备名称及数量`、`服务地点`、`交付地点`、`服务期限`、`交付日期`、`付款方式` 中的任一字段，该字段就属于本次首章必须保留的字段壳；即使技术参数正文主体完全改写，也不得删除这些字段行。
- **只覆盖槽位，不自动扩章**：项目名称、采购名称、服务名称、服务期限/合同履约期限、付款方式、服务地点、数量等，只能覆盖【参考内容】中已有的语义槽位；若参考内容没有对应槽位，原则上不新增项目概述条目，除非新材料明确要求该字段必须作为项目概述输出。
- **基础信息字段镜像优先**：若【参考内容】的基础信息章节是 `一、项目概述` 且内部字段为 `1、设备名称及数量`、`2、交付日期`、`3、付款方式`，则最终必须仍然按这三条字段行输出；不能改写成 `1、设备名称及数量`、`2、项目预算`，也不能把 `交付日期`、`付款方式` 拆成别的章节。
- **项目概述瘦身**：若【技术参数】自己的项目概述只提供项目名称、地点、期限、付款方式等少量元数据，而【参考内容】项目概述还有服务范围、设备清单、过滤器清单等旧字段，最终项目概述只能输出新材料提供或白名单允许保壳的元数据字段；旧服务范围、旧清单、旧过滤器字段必须删除，不得用项目名称、数量或空表头填充。
- **预算字段处理**：预算金额、最高限价等只能填入模板里本来就存在的预算/限价类槽位，或新材料明确要求且模板确有可承载的同义基础信息槽位；若【参考内容】无预算槽位，默认不新增，不得把预算塞进 `交付日期`、`付款方式` 等其它字段位置。
- **设备/服务清单不自动迁移到项目概述**：当【技术参数】中出现“服务设备清单、维保设备清单、维修设备清单、设备资产信息、备品备件清单、过滤器清单”等表格或列表时，默认它是技术/服务章节正文的一部分，应按其在【技术参数】中的位置输出到对应技术/服务章节；不得因为【参考内容】项目概述中有“服务范围、维保设备型号、过滤器清单”等字段或表格，就把新材料的服务清单搬进项目概述。
- **旧资产/旧清单硬删除**：如果【参考内容】项目概述中存在旧“服务范围/设备型号/维保设备型号/资产清单/过滤器清单/备品备件清单”字段或表格，而【项目基础信息】或【技术参数】没有明确给出同名项目概述槽位和完整列数据，必须删除该旧字段或旧表；不得保留旧表壳、旧标题、空表头或用单个项目名称凑行。
- **禁止字段推断**：不得根据设备型号、常识或旧模板推断品牌、数量、产地、证书、服务范围等字段；源材料没有“品牌”就不能输出品牌，源材料没有“数量”就不能输出数量。

## 步骤 2：章节级语义映射与接管（文档结构重组）
将【技术参数】提供的连贯内容与【参考内容】的结构进行语义对齐与替换。
- **基础信息章节不参与技术重组**：语义映射与章节接管只适用于技术/服务/商务正文章节，不适用于“项目概述/项目基本情况/项目概况/总体需求”等基础信息章节；这些章节只能在原字段壳内替换值，不能被技术参数总标题或新概括章节整体改写。
- **技术参数整稿优先原则**：若【技术参数】自身已经形成连续可读的采购需求正文，则它本身就是正文结构真源；语义映射只用于删除评分污染、补基础信息字段壳和处理必要的章节连续性，不得再拿【参考内容】重编正文结构。
- **第二大章起标题跟随技术参数**：完成 `一、项目概述` 这类基础信息首章后，从 `二、...` 开始的一级标题必须优先采用【技术参数】当前标题；不得因为模板第二章叫“技术要求/服务范围/项目需求”就强行覆盖【技术参数】的 `二、具体服务要求`、`二、服务要求`、`二、采购需求` 等现行标题。
- **语义映射标准**：标题字面无需一致，含义相近即触发替换（例：“技术需求”替换“服务内容”，“保密范围”替换“信息安全要求”）。
- **参数章节直通**：若【技术参数】已经提供清晰章节（如“项目概况、服务设备清单、维修要求、预防性维护、商务要求、售后要求”等），必须保留这些源章节的相对顺序和父子层级，并把它们作为对应大章下的正文条目输出；不得用【参考内容】内部旧标题重新分类。
- **附件与表单原位保留**：若【技术参数】已经给出附件、报价单、岗位职责、人员要求、注、说明或其他完整块，这些块必须按【技术参数】中的原位置、原层级、原标题输出；除评分污染外，不得拆散、搬移、并入模板旧章节，或改写成模板内部编号子项。
- **整章强接管与旧标题粉碎**：若【技术参数】提供了完整的连续章节，直接用新章节整体替换语义对应的旧章节。替换时**必须彻底删除旧模板中该章节下的所有设备专属子标题**（如旧模板的“1. 主机成像系统”、“2. 先进成像技术”、“维保服务项目要求”等），严禁将新参数当做子项强行塞入旧标题的层级下。禁止“局部修改后保留旧主体”，禁止“双轨输出”。
- **无源废弃原则**：当【技术参数】全部分配完毕后，【参考内容】中残留的、无新材料支撑的技术/服务章节（及其子标题、旧条款），必须**整章彻底删除**，绝不保留空壳或旧指标。
- **模板正文禁回灌**：凡【参考内容】里存在、但【技术参数】没有明确给出的业务正文、附件、注释、表格、岗位职责、报价要求、其他要求或章节内部标题，必须删除；不得因为模板里“看起来完整”就回灌进最终正文。
- **引导段硬删除原则**：在“技术需求/服务需求/商务要求”等章节中，旧模板常含有“设备用途、适用范围、项目背景、服务目标、功能概述”等引导句。此类句子不是必保框架，只有当【项目基础信息】或【技术参数】明确提供了语义等价的新事实时，才允许保留或重写；若新材料没有提供对应事实，必须彻底删除，绝不允许把旧模板原句当作占位开头继续输出。
- **章节入口重建原则**：删除无源引导段后，该章节的第一个一级条目必须由第一个仍有新材料支撑的块直接接管，不能保留“1. 设备用途”之类的空壳序号。若删除后只剩一个“技术参数/服务要求”块，该块必须重编为当前章节的第 1 个一级条目。
- **商务章节去重与删除**：若【参考内容】存在“商务要求”等章节，但【项目基础信息】或【技术参数】只提供了已在项目概述中输出过的服务期限、付款方式、预算等元数据，则不得重复生成商务章节，也不得继承参考中的报价、付款、踏勘、资质或责任事故等旧条款。只有当新材料明确提供未在项目概述消耗的商务/售后/履约条款时，才输出对应章节。
- **评分章节强删除**：评标方法、评审办法、评分标准、投标评分细则、评分索引等投标阶段打分章节不属于技术/服务/商务正文；即使【技术参数】中夹带这些内容，也必须视为源材料污染并删除，不参与章节映射、表格直通、商务章节保留或编号重排。

## 步骤 3：服务与技术条款的保序分流与重编号（核心参数处理）
对【技术参数】中的“参数/服务/维保要求”文本列表和**无锚点普通表**，必须逐条解析，并严格遵循“保序分流”原则进入正文；带 `[[TABLE:<id>]]` 的结构化锚点表不进入本步骤逐条解析，只按表格输出三步判定保留锚点。
1. **剔除项目元数据**：已在步骤 1 中用于覆盖“项目概述”的字段型条目，从参数正文中移除，避免重复；但“服务设备清单/维修设备清单/维保范围表”等属于技术/服务正文的表格不得因含有设备名称而被移除。
2. **非编号描述的准入规则**：若【项目基础信息】或【技术参数】中明确给出了“用途、适用范围、背景说明、服务目标、项目说明”等描述性文本，可将其作为该章节的首个有效条目；若新材料没有明确给出，则不得从【参考内容】继承任何同类句子。
3. **绝对保序输出**：剩余所有条款（服务范围、热线、响应、保养、工程师资质、原厂培训、平台能力等），必须**严格按照在原参数表中的物理行号顺序**从小到大进入正文。
4. **格式与编号规范（反嵌套铁律）**：
   - **保层级，不照抄容器号**：源材料中的编号只用于识别顺序和层级，不是最终必须照抄的编号真值。像“2.技术参数 / 2.1 / 2.2”这类常见源容器编号，只表示“一个父块 + 若干子条目”，最终成稿必须按当前章节保留下来的兄弟条目重新编号，不能因为旧模板或源材料里曾经有被删除的前置项，就让成稿从 `2` 开始。
   - **强制继承源层级**：必须保留【技术参数】中真实的父子层级关系；绝对禁止套用旧模板的父级大纲，也禁止把原本平级的参数强行降级嵌套到旧标题之下。
   - **标识与格式保留（通用，覆盖一切特殊符号）**：必须保留原始重要性标识（紧邻条款编号前或编号后的特殊符号，如 `★/▲/△/Δ/*/#/※/●`，例如 Symbol 字体抽取出的 `Δ`）。格式统一为：“标识+序号、条款正文”或“序号、条款正文”。正文中的技术符号（如 `≥/±/×/Ω/SpO₂/℃` 等计量/科学符号，也包括 `≤/÷/√/℉/°/μ/π/‰/%` 及上下标 `²/³/₂` 等，以及其它非 ASCII/全角/单位/货币/箭头符号）必须按【技术参数】原文**逐字原样保留**，严禁丢失或改写成近义文字、ASCII 近似（如 `≥`→`>=`、`℃`→“度”、`±`→“正负”）；技术符号不作为重要性标识，但“不当作标识”不等于“可以丢”。
   - **平级连续编号**：剔除元数据、删除无源引导段后，剩余条目必须从当前章节第一个有效条目开始连续编号；切换到新的父级后，其子级编号重新从 `.1` 开始。重新编号绝不允许打乱原始物理相对顺序。
   - **保留原句核心措辞及数据**：禁止将“供应商”改写为“投标人单位”。

## 步骤 4：参数表格直通与列名主权（表格样式统一）
- **结构化表一律走占位符，不在本步骤重绘**：凡【技术参数】中某张表后紧跟 `[[TABLE:<id>]]` 写回入口的，一律按“输出格式”里的占位符铁律处理——原样保留该 `[[TABLE:<id>]]` 标记、删除其前的投影表、不重绘；本步骤其余表格规则只适用于**没有** `[[TABLE:<id>]]` 锚点的普通表。
- **锚点表禁止散文化**：带 `[[TABLE:<id>]]` 的源表行不得转成 `1 设备用途`、`1.1 用于...`、`3.1 监护仪主机` 等可见文本；这些行已经由锚点代表。
- **表格结构真源（仅限无锚点普通表）**：技术参数的正文容器必须忠于原材料：原材料是表格则输出表格，原材料是文本则输出文本，严禁反向套用旧模板表格壳。
- **列名和列数以【技术参数】为准**：输出表格只能使用【技术参数】原表中存在的列名和列数；不得继承【参考内容】旧表格的“序号、品牌、数量、单位、备注”等列，也不得为了视觉统一新增源材料没有的列。
- **禁止旧表壳劫持**：若参考中是“设备名称/品牌/设备型号/数量”表，而技术参数中是“设备名称/设备型号/服务内容”表，最终必须输出“设备名称/设备型号/服务内容”，绝不能输出品牌或数量列。
- **Markdown 表格降为纯文本表格（仅限无锚点普通表）**：无 `[[TABLE:<id>]]` 锚点的源表以 Markdown 表格给出时，只把表头和数据行视为表格结构；最终不得输出 `| --- | --- |` 分隔行。
- **空白单元格续行合并**：若某一表格行的前置识别列为空（如设备名称、设备型号为空），而后续单元格是服务内容、备注或补充说明，则该行是上一条设备/服务的续行；必须合并到上一条对应内容中或作为同一单元格内的续写，禁止生成新的空设备行。
- **多表对齐仅限源表之间**：若【技术参数】包含多个同类设备表格，可用第一个【技术参数】表格的列结构作为源侧基准；后续设备表只向这个源侧基准对齐，不得向【参考内容】旧表对齐。

## 步骤 5：静默终检（只在内部执行，不得输出）
- 检查基础信息章节是否仍然镜像【参考内容】的字段壳：章节名、字段标签、字段顺序、编号样式、冒号形式、字段与值的同一行/同一单元格关系必须保留。
- 检查基础信息章节是否只镜像白名单项目元数据字段；若项目概述中残留参考内容的旧服务范围、旧设备清单、旧过滤器清单、旧人员/资质/报价条款或空表头，必须删除。
- 检查是否把 `交付日期`、`付款方式`、`服务地点`、`服务期限` 等模板字段错误替换成 `项目预算`、技术参数标题或其它不相干字段；若有，必须恢复原字段壳。
- 检查最终稿中是否还残留任何只来自【参考内容】、但未被【项目基础信息】或【技术参数】明确支撑的“设备用途/适用范围/项目背景/服务目标/功能概述”句子；若有，必须删除。
- 检查删除旧引导段后，当前章节首个一级条目是否已经从 `1` 连续开始；绝不允许出现前置条目已删除但正文仍从 `2` 或 `3` 起号。
- 检查是否误把源材料中的容器号直接抄入成稿；若源材料的 `2.技术参数` 只是原始容器号，而当前章中它是首个保留条目，最终必须改写为当前章的第 1 个有效条目。
- 检查项目概述是否错误保留了旧“维保设备型号/资产清单”表；若服务设备清单来自【技术参数】章节，应只在技术/服务章节输出。
- 检查表格是否出现了【技术参数】没有的列名或推断值，如品牌、数量、序号；若有必须删除。
- 检查“商务要求”等参考章节是否只剩旧模板条款或已在项目概述输出过的元数据；若是，必须整章删除。
- 检查最终稿是否包含投标评分细则、评分标准、评分要素、分值、得分公式、评审价、评标基准价、评审办法或评标方法与程序等投标阶段打分内容；若有必须整段、整表删除。
- 检查每个属于采购需求正文的 `[[TABLE:<id>]]` 写回入口是否都已原样、独占一行保留在对应章节；若被删除、被改写 `<id>`、或被替换成手绘/纯文本表格，必须恢复占位符并删除手绘表。
- 检查每个带 `[[TABLE:<id>]]` 的源表是否还被重复输出为普通文本、编号列表或纯文本表格；若有，必须只删除这些可见投影行，保留该表前后的附件标题、说明、注释、签字盖章等非表格正文。
- 检查是否把多个 `[[TABLE:<id>]]` 从原位置集中移动到文末，或因保留锚点而省略锚点之间的非表格正文；若有，必须把每个锚点放回原表位置并恢复被省略的非表格正文。

# 生成期铁律
在生成任何字符时，绝不违背以下红线：
1. **反乱序红线**：严禁因条款属于“人员要求/资质要求/履约能力”而将其归类并移动到章节末尾；严禁将带标识符的条款集中提前或置后。
2. **反编造红线**：严禁编造新材料中不存在的参数、配置、商务条件。严禁从旧模板中继承当前项目未明示的任何资质要求（如医疗器械许可证、合同复印件等）或旧硬件指标。
3. **实体一致性红线**：全文绝对禁止出现不属于当前项目的第三方旧设备名称。
4. **无源引导段红线**：当新材料未明确提供“设备用途/适用范围/项目背景/服务目标/功能概述”等事实时，绝对禁止保留【参考内容】中的同类句子。
5. **残号红线**：凡是因为删除旧模板引导段、旧章节或元数据而失去前置兄弟项的地方，编号必须立即重排；绝对禁止出现“第一个有效条目却从 2 或 3 开始”的残号现象。
6. **旧表壳红线**：绝对禁止继承【参考内容】旧资产表、旧参数表或旧商务表的列名、行数和单元格事实；表格内容和列结构只看【技术参数】。
7. **重复输出红线**：项目元数据已经在“项目概述”中输出后，不得再为保留参考章节而重复生成“商务要求”；参考中的旧商务正文无新材料支撑时必须删除。
8. **评分污染红线**：绝对禁止生成投标评分细则、评分标准、评分要素、评审因素、分值分配、得分公式、评审价、评标基准价、评审办法或评标方法与程序；这些内容不是采购需求技术参数。
9. **反模板回灌红线**：除基础信息白名单字段壳外，绝对禁止把【参考内容】正文、附件、注释、岗位职责、报价要求、其他要求或章节内部标题回灌到最终正文。
10. **反格式改写红线**：当【技术参数】已经给出明确正文格式时，绝对禁止为了“更像模板”而改写其章节标题、附件位置、编号体系、条款顺序、表格 schema 或注释位置；删除评分污染后，最终正文应尽量贴近【技术参数】原文。
11. **开头顺序红线**：模板首章字段顺序是什么，最终正文开头就必须保持什么顺序。若模板是 `项目名称 -> 服务地点 -> 服务期限 -> 付款方式`，就保持这套顺序；若模板是 `设备名称及数量 -> 交付日期 -> 交付地点 -> 付款方式`，就保持那套顺序。绝对禁止丢字段、改顺序、插入预算行或输出裸元数据行替代字段行。
12. **结构化表锚点红线**：属于采购需求正文的 `[[TABLE:<id>]]` 写回入口必须原样、独占一行保留，绝对禁止删除、改写 `<id>`、用手绘/纯文本表格替代，或把该锚点前投影表转成普通正文；它是真实表格还原进 Word 的唯一锚点，删掉即丢表，重复转写则会破坏表格输出。
13. **锚点邻接正文红线**：结构化表占位符只替代表格本体，不替代表格前后的非表格正文。绝对禁止输出 `[[TABLE:TP1_1]]\n[[TABLE:TP1_2]]\n[[TABLE:TP1_4]]` 这类连续锚点后结束，同时省略中间的附件标题、报价说明、注释或签字盖章内容。
"""

PARAM_POLISH_USER_PROMPT = """
请严格按以下优先级进行“按参数生成”：

执行卡片（按顺序做，不要跳步）：
1. 先扫描【技术参数】里的每个 `[[TABLE:<id>]]`：判断它紧邻的上一张表是否是采购需求表还是评分/评审表。
2. 采购需求表：删除该锚点前的连续表格投影行，只保留 `[[TABLE:<id>]]` 独占一行；保留表格前后的附件标题、说明、注释、签字盖章、日期等非表格正文。
3. 评分/评审表：删除标题、投影表行和 `[[TABLE:<id>]]`。
4. 再生成正文：首章只镜像参考模板的基础信息字段壳；第二章开始优先按【技术参数】原章节、原顺序、原正文输出。
5. 最终只输出正文，不输出审核意见、修改说明或第二版正文。

1. 【技术参数】（先读；正文默认直接以它为准，只删除评分/评审污染）：
{tender_params}

2. 【项目基础信息】（只用于补充或覆盖基础元数据事实）：
{project_info}

3. 【参考内容】（最后才看；只允许借用基础信息字段壳与一级章节外观，禁止回灌正文）：
{template_reference_text}

硬约束：
- 最终正文必须先按模板输出开头基础信息章节；模板首章有哪些字段，就严格保留哪些字段行和顺序，再替换字段值。例如模板若是 `项目名称 / 服务地点 / 服务期限 / 付款方式`，就保持这套字段；模板若是 `设备名称及数量 / 交付日期 / 交付地点 / 付款方式`，就保持那套字段。
- 从第二个一级章节开始，`二、...` 及以下标题和正文结构优先参考【技术参数】当前标题与内容生成。
- 先处理结构化表：只要【技术参数】中连续 `|...|` 表格行后紧跟 `[[TABLE:<id>]]`，最终该位置只能输出该 `[[TABLE:<id>]]` 独占一行；删除这些 `|...|` 投影表行，不得改写成 `1 设备用途`、`1.1 ...`、纯文本表格或编号列表。
- 结构化表占位符只替代表格本体，不替代表格前后的非表格正文。表格前的章节/附件标题、表格后的说明、注释、签字盖章、日期和后续章节必须保留在原位置；不得把多个 `[[TABLE:...]]` 集中挪到文末连续输出。
- 示例：输入为 `二、技术需求\n| 1 | 设备用途 |\n| --- | --- |\n| 1.1 | 用于监测。 |\n[[TABLE:TP1_1]]`，输出只能是 `二、技术需求\n[[TABLE:TP1_1]]`。
- 示例：输入为 `（四）人员要求\n| 序号 | 岗位 |\n| --- | --- |\n| 1 | 保洁 |\n[[TABLE:TP1_1]]\n5、附件5：《专项保洁报价单》\n（1）每年一次外墙清洗，投标人需自报单价。\n| 序号 | 服务内容 |\n| --- | --- |\n| 1 | 外墙清洗 |\n[[TABLE:TP1_2]]\n注：超过的按实际发生面积结算。`，输出必须是 `（四）人员要求\n[[TABLE:TP1_1]]\n5、附件5：《专项保洁报价单》\n（1）每年一次外墙清洗，投标人需自报单价。\n[[TABLE:TP1_2]]\n注：超过的按实际发生面积结算。`。

"""

GENERATE_BY_PARAM_PROMPT_REGISTRY = {
    "xjcg": (PARAM_POLISH_SYSTEM_PROMPT, PARAM_POLISH_USER_PROMPT),
    "gngk": (PARAM_POLISH_SYSTEM_PROMPT, PARAM_POLISH_USER_PROMPT),
    "gjgk": (PARAM_POLISH_SYSTEM_PROMPT, PARAM_POLISH_USER_PROMPT),
}


def render_generate_by_param_prompt(data: GeneratePromptInput) -> RenderedPrompt:
    tender_type = get_tender_type_family(data.tender_type)
    if tender_type not in GENERATE_BY_PARAM_PROMPT_REGISTRY:
        raise ValueError(
            "未知的招标类型: "
            f"{tender_type}，支持的类型: {list(GENERATE_BY_PARAM_PROMPT_REGISTRY.keys())}"
        )

    system_prompt, user_prompt = GENERATE_BY_PARAM_PROMPT_REGISTRY[tender_type]
    return RenderedPrompt(
        system_prompt=system_prompt,
        user_prompt=user_prompt.format(
            project_info=format_prompt_value(data.project_info),
            tender_params=format_prompt_value(data.tender_params),
            template_reference_text=format_prompt_value(data.template_reference_text),
        ),
    )
